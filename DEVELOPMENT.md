# Development Notes

## VS Code

- Rust inlay hints are configured to stay hidden until `Ctrl+Alt` is held.
- Workspace setting: `"editor.inlayHints.enabled": "offUnlessPressed"` in `.vscode/settings.json`.
