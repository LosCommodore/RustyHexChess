{
  "name": "rusty-hex-chess",
  "version": "0.1.0",
  "private": true,
  "description": "Hexagonal chess engine in Rust with TypeScript frontend",
  "scripts": {
    "build:engine": "wasm-pack build engine --target web --release",
    "build:engine:dev": "wasm-pack build engine --target web --dev",
    "build:frontend": "cd frontend && quasar build",
    "build": "npm run build:engine && npm run build:frontend",
    "dev": "npm run build:engine:dev && cd frontend && quasar dev",
    "clean": "cargo clean && rm -rf engine/pkg frontend/dist frontend/.quasar"
  },
  "keywords": ["hexagonal chess", "wasm", "rust"],
  "author": "Christian Langen",
  "license": "MIT"
}
